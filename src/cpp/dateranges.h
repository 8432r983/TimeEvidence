#ifndef DATERANGES_H
#define DATERANGES_H

#include <QObject>

class DateRanges : public QObject {
    Q_OBJECT
  public:
    explicit DateRanges(QObject *parent = nullptr);

  signals:
};

#endif // DATERANGES_H
